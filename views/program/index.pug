extends ../layout

block content
  h1= title
  p Welcome to #{title}
  // Add new program program form
  form(method='POST' action='program/submitNewProgram')
    br
    div.form-group
      br
      label(for='program') Type Program Name: 
      input#program.form-control(type='text', placeholder=' Enter program name' name='program')
      a.btn.btn-primary.btn-lg(type='submit') Add New Program


  ul.list-group
    each program in programs
      li.list-group-item
        a(href="/program/" + program._id)= program.Program 
        div.table-wrapper
          table.desc
            tr
              th Exercise
            each item in program.Exercise
                tr
                  td(height='50px')=item

          table.desc
            tr
              th Description
            each item in program.Description
                tr
                  td(height='50px')=item

          table.desc
            tr
              th Set
            each item in program.Set
                tr
                  td(height='50px')=item

          table.desc
            tr
              th Reps
            each item in program.Reps
                tr
                  td(height='50px')=item




